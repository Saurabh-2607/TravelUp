/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    domains: ['images.unsplash.com','images.pexels.com','picsum.photos', 'cdn.pixabay.com','img.freepik.com'],
  }
};

export default nextConfig;
